version: "2"
sql:
  - engine: sqlite
    schema:
      - migrations
      - schema/viewing_service.sql
    queries: queries
    gen:
      go:
        package: store
        out: internal/store
        emit_empty_slices: true
